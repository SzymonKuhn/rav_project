package pl.sda.rav;

public class Amphibian extends Vehicle {
    public Amphibian(int vin, String name, String productionDate) { // konstruktor
        super(vin, name, productionDate);
    } //koniec konstruktora
}
